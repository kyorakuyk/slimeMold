## 2026-09-05 只读项目调研（无代码改动）
- 应用户要求只读了解项目，未修改任何项目文件。
- 发现 CODEBUDDY.md 与实际已漂移：项目现已有 vitest 测试框架（package.json `test` 脚本），engine/ 已演化为 executor/builder/runLoop/scheduler/checkpoint 等多文件结构，且存在 src/dev、src/projectControl、src/domain 等文档未提及目录（另一 agent 正在开发中，约 24 个文件未提交，方向为项目会话/Codex worktree/worker 执行 + 宿主隔离 + GUI 验收）。
## 补充：用户背景与教学
- 用户自述是学生、纯新手、无项目/工程经验；已用餐厅比喻 + 三张 SVG 图讲解：节点式工作流概念、三层架构（React UI / Zustand+引擎 / Tauri Rust 壳）、拓扑排序执行流程。
- 建议学习路线：JS → TS → React → 按 types.ts → nodes → topoSort 顺序读源码 → 最后再看 Rust。
- 长期偏好：讲解需从零基础出发、多用类比，避免术语堆砌。（值得写入长期记忆）
## 补充：App.tsx 依赖讲解
- 逐类讲解了 App.tsx 的 57 行 import：react hooks/xyflow/lucide 三方库、14 个 components UI 零件、发动机零件（store/io/nodes/plugins/platform/env）、WorkflowEditor 画布、projectControl/+domain/ 新开发区（引用最密集）。
- 讲了新手要点：import 语法、useState/useEffect/useRef、import type、动态 import、模块顶层副作用 registerBuiltins()。
- 观察点：App.tsx 已 1207 行，worker 调度逻辑（runQueuedWorker/cleanupWorkerRun 等）宜拆到 projectControl/；LeftSidebar 已改版为 SideRail/SidePanel 图标条结构，CODEBUDDY.md 描述仍为旧版。
