# SlimeMold 使用指南

一份面向使用者的操作手册。技术细节、插件开发与部署见 `README.md`。

---

## 1. 添加节点

- **从左侧节点库拖入**：按住节点库里的任意节点，拖到画布后松开即可创建。（单击节点库不会再生成节点，必须拖入。）
- **双击空白画布**：在画布空白处双击，会弹出「添加节点」窗口（仿 ComfyUI 风格）。
  - 顶部搜索框输入关键字，下方按网格列出匹配的节点 / 子图；
  - 用 **↑ / ↓** 选择，按 **回车** 或**点击**即可在双击位置创建节点；
  - 窗口右上角 **✕** 或按 **Esc** 关闭，点击窗口外的灰色遮罩也能关闭。

---

## 2. 连线与编辑

- **连线**：把鼠标移到节点边缘的端口上，按住左键拖到另一节点的端口松开。端口按数据类型着色，类型不匹配会被拒绝。
- **框选**：点击左侧工具栏的「框选」按钮后，在画布上拉出矩形，圈中的节点会被选中。
- **移动**：直接拖动节点；拖动已编组的整体卡片可一并移动组内成员。
- **删除节点**：选中后按 `Delete` / `Backspace`。
- **运行**：顶栏「运行」按钮执行整个工作流；节点会显示灰（待运行）→ 转圈（运行中）→ 绿（成功）/ 红（失败）。

> 工作流结构为 DAG（有向无环图），引擎会自动拦截环路。

---

## 3. 智能体配置

1. 顶栏「智能体」面板中新建一个智能体，选择协议（OpenAI 兼容 / Anthropic / Ollama 本地）、填写 Base URL、API Key 与模型。
2. 选中画布上的「智能体」节点，在右侧属性面板里绑定刚才配置的智能体。
3. 点「运行」。

Ollama 本地模型无需 API Key，详见 `README.md` 的「Ollama 本地模型接入」。

---

## 4. 编组（把节点框成一组）

适合把一段相关节点收拢起来、整体移动或折叠，方便整理画布。

1. 用**框选**选中若干节点；
2. 在画布上**右键**，选择 **「编为一组」**；
3. 得到一个新的分组：
   - **展开态**：显示为带标题条的卡片框，框内节点标着色点。点标题条右侧的 **✕** 可解散分组（节点保留）。
   - **折叠态**：点「展开」按钮收起后，组会变成一个节点（代理节点），**左右两侧自动生成彩色端口**（按内部端口类型聚合，例如内部需要 2 个图片 + 1 个文本输入，则左侧只出现「图片」「文本」两个端口）。外部连线接到这些端口，会映射到组内的对应节点。
   - **进入子图**：在折叠态卡片上**双击**，进入子图编辑视图——左侧列出输入代理端口、右侧列出输出代理端口，中间是组内节点，可继续连线。
   - **退出子图**：子图视图右上角「完成并返回」。
   - **解散分组**：折叠态卡片上的 **✕** 按钮，或左侧「我的分组」面板里对应的垃圾桶图标。

> 编组只影响画布整理，**不改变运行逻辑**。

---

## 5. 打包为子图（复用节点组合）

适合把一段流程做成「可复用的单个节点」，像 ComfyUI 的组节点一样反复拖用。

1. **框选**若干节点后，在画布上**右键**，选择 **「打包为子图」**；
2. 这段流程会被抽象为一个子图定义，并生成一个**子图引用节点**留在画布上（双击该节点可展开回内部）；
3. 该子图会出现在左侧节点库底部的 **「我的子图」** 分区：
   - **拖入画布** 即可再放一个引用实例；
   - **双击**子图项可**重命名**；
   - 悬停出现的 **🗑 垃圾桶** 可**删除**该子图（画布上已放置的引用会失效）。

> 与「编为一组」的区别：编组是就地折叠整理；打包为子图会产出一个可复用、可多次实例化的独立节点。

---

## 6. 导入 / 导出

- **导出**：顶栏「导出」把当前工作流保存为 `.workflow.json`（含节点、连线、参数与智能体配置）。
- **导入**：顶栏「导入」加载之前导出的文件。仓库内 `examples/demo.workflow.json` 是一个最小演示，可直接导入体验。

---

## 7. 规划节点（dispatch.plan）与调试模式

### 7.1 规划 / 编排节点

`dispatch.plan`（左侧节点库「派发」分类）用于把一个**目标**拆解成可执行的**任务清单**，是「规划 → 派发」工作流的起点。

- **输入**：`goal`（目标文本）
- **输出**：`plan`（计划书）、`tasks`（任务清单，每个任务含 `title` / `scope` / `payload` / `index`）、`summary`（摘要）
- **关键参数**：
  - **绑定规划智能体**：选择已配置的智能体，由其生成规划内容
  - **格式**：`tasks`（默认，输出结构化任务数组）或 `free`（仅输出计划书文本）
  - **模型覆盖**：留空则用智能体默认模型，填了则临时覆盖
  - **模拟模式（simulate）**：选 `on` 时**不调用 LLM**，直接回显一个示例规划链路，用于快速验证连线与下游配套，无需真实 API Key

典型链路：`文本输入` → `dispatch.plan` → （`tasks` 接 `派发/拆分`节点做并行施工，或 `结果预览` 查看）。

### 7.2 调试模式（虫子按钮）

顶栏「运行」按钮左侧有一个 **🐛 虫子图标** 按钮，用于切换**调试模式**：

- **关闭（默认）**：画布节点卡片保持简洁，不显示调试动作。
- **开启**：按钮高亮，节点运行出「成功 / 失败 / 已缓存」状态后，卡片底部出现两个调试动作：
  - **重跑子图**：从当前节点开始增量重跑它及其下游（等价于引擎的 `incremental` 续跑，跳过未变上游）。
  - **重跑到此节点**：执行到该节点为止，其下游被标记为 `skipped`（等价于 `stopAfterNodes`）。适合定位「在哪一步之前链路是好的」。

> 调试模式偏好会被记住（持久化），下次启动沿用上次设置。它只影响调试动作的显隐，**不改变工作流本身的运行逻辑**。

---

## 常用快捷键速查

| 操作 | 方式 |
| --- | --- |
| 添加节点 | 从节点库拖入，或双击空白画布 |
| 选中多个节点 | 工具栏「框选」后拉矩形 |
| 删除节点 / 分组 | 选中后 `Delete` 或 `Backspace` |
| 打开节点选择窗 | 双击空白画布 |
| 关闭弹窗 | `Esc` / 点 ✕ / 点窗外遮罩 |
| 进入子图编辑 | 双击折叠的分组卡片 |
| 退出子图编辑 | 子图视图「完成并返回」 |
| 编为一组 | 框选后右键 → 编为一组 |
| 打包为子图 | 框选后右键 → 打包为子图 |
| 切换调试模式 | 顶栏「运行」左侧的 🐛 虫子按钮 |
| 重跑子图 / 重跑到此节点 | 开启调试模式后，节点卡片底部的调试动作 |
