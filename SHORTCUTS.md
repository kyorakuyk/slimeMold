# SlimeMold 快捷键速查表

> 用于测试与日常使用。Mac 上 `Ctrl` 对应 `⌘ Cmd`。

## 全局快捷键

| 快捷键 | 作用 |
| --- | --- |
| `Ctrl/Cmd + Shift + N` | 新建项目 |
| `Ctrl/Cmd + N` | 新建工作流（当前项目内） |
| `Ctrl/Cmd + S` | 保存项目（.smproj） |
| `Ctrl/Cmd + =` / `+` | 放大画布视图 |
| `Ctrl/Cmd + -` | 缩小画布视图 |
| `Shift + 1` | 适配窗口（缩放到全部节点） |
| `Delete` / `Backspace` | 删除所有选中的节点与连线（框选多选后整体删除） |
| `Esc` | 关闭打开的下拉菜单 / 弹窗 |
| `Ctrl/Cmd + K` / `Ctrl/Cmd + Space` | 唤起节点命令面板（搜索并插入节点，落于视图中心） |
| `Ctrl/Cmd + Z` | 撤销最近一次图结构变更（增删节点/连线/分组/子图） |
| `Ctrl/Cmd + Shift + Z` 或 `Ctrl/Cmd + Y` | 重做 |
| `Ctrl/Cmd + C` | 复制选中节点（含内部连线）到剪贴板 |
| `Ctrl/Cmd + V` | 粘贴剪贴板（新 id + 偏移） |
| `Ctrl/Cmd + D` | 克隆选中节点（复制并立即粘贴） |
| `Ctrl/Cmd + A` | 全选所有节点 |
| `Ctrl/Cmd + B` | 切换选中节点的 bypass（跳过执行，同名端口透传） |
| `Ctrl/Cmd + M` | 切换选中节点的 mute（完全屏蔽，不执行） |

## 画布交互（React Flow 默认）

| 操作 | 作用 |
| --- | --- |
| 滚轮 | 缩放 |
| 拖拽空白处 | 平移画布 |
| 拖拽节点 | 移动节点 |
| 拖拽端口连线 | 建立节点间连接 |
| 拖拽已有连线的一端 | 重新连线到其它端口（自动环路 + 类型校验） |
| 双击空白画布 | 弹出节点选择窗（在鼠标处插入） |
| 双击节点 | 展开 / 收起节点下方详情（默认折叠） |

## 画布浮层工具栏

- **左上角（对齐 / 分布）**：先框选 ≥2 个节点，再点按钮——左对齐 / 水平居中 / 右对齐 / 顶对齐 / 垂直居中 / 底对齐 / 水平等距分布 / 垂直等距分布（支持撤销）。
- **左下角（模式 / 小地图）**：拖动 / 框选 / 点击 三种交互模式切换，小地图显隐。
- **右下角（缩放）**：缩小 / 当前缩放 / 放大 / 适应视图。

## 菜单栏（顶部）

点击 **文件 / 编辑 / 视图 / 运行** 展开二级菜单：

- **文件**：新建工作流 · 导入 JSON… · 导出 JSON
- **编辑**：删除选中节点（未选中时置灰）· 清空画布（空画布时置灰）
- **视图**：放大 · 缩小 · 适配窗口 · 显示/隐藏节点库 · 显示/隐藏底部面板 · 切换深色/浅色主题
- **运行**：运行 / 停止工作流

## 面板拖拽

- 左侧分隔条：拖动调节**节点库**宽度（160–480px）
- 右侧分隔条：拖动调节**属性面板**宽度（160–480px）
- 底部分隔条：向上拖动调节**底部面板**高度（120–480px）
